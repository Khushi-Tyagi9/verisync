[project]
name = "verisync"
version = "0.0.0"
description = "Real-time reconciliation between Razorpay payment status and a merchant's order record"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra"
